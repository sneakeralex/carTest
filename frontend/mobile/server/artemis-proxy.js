// server/artemis-proxy.js
// REMOVED: the development Artemis signing proxy was removed per repository policy.
// This placeholder prevents accidental execution. To fully remove the file from git run:
//   git rm server/artemis-proxy.js && git commit -m "remove artemis dev proxy"

export default function removedProxy() {
  throw new Error('server/artemis-proxy.js has been removed. Delete the file from the repository with `git rm server/artemis-proxy.js`');
}
