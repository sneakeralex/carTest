// 模拟车辆数据 — 已移除，使用空数组以避免在测试/运行时泄露真实数据
export const mockVehicles = [];

// 模拟图片上传响应（保留）
export const mockImageUpload = {
  url: 'https://example.com/uploaded-image.jpg',
  filename: 'vehicle-image.jpg',
  size: 1024 * 1024,
  type: 'image/jpeg'
};
